export enum ApiKeyIn {
  Query = 'query',
  Header = 'header',
  Cookie = 'cookie'
}

export enum SecurityAuthTypes {
  Bearer = 'http',
  Basic = 'http',
  ApiKey = 'apiKey',
  Oauth2 = 'oauth2'
}

export enum SecurityAuthScheme {
  Bearer = 'bearer',
  Basic = 'basic'
}

