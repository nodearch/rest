const METADATA_PREFIX = 'nodearch-rest:';

const METADATA_KEY = {
  ARCH_ROUTE_INFO: 'route-info',
  ARCH_MIDDLEWARE: 'middleware',
  ARCH_VALIDATION_SCHEMA: 'validation-schema'
};

export {
  METADATA_KEY,
  METADATA_PREFIX
};